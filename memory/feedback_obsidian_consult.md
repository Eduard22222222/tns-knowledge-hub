---
name: feedback-obsidian-consult
description: Always consult the Obsidian vault(s) at the start of substantive TNS/client/project work — pull relevant context + skills and mention them up front.
metadata: 
  node_type: memory
  type: feedback
  originSessionId: b833b2f7-3be7-472e-a90a-c40b90a28f50
---

When starting substantive work on a TNS / client / ComfortMap / internal project, **go through the Obsidian vault(s) first** and pull the relevant context + skills, then **explicitly mention to Eduard which notes/skills I'm using**. The vaults are NOT auto-loaded into my context — I must actively read them; otherwise I miss things that are already written down.

**Why:** 2026-06-30, during a ComfortMap deploy I went to open the GitHub tokens page while the browser was on the wrong account (Eduard22222222). The correct-account fact ("comfortmap → thenichesociety, never Eduard22222222") was already written down, but I didn't surface it proactively. Eduard's point: nothing auto-reads Obsidian, and even the auto-memory facts must be APPLIED, not just held.

**How to apply:**
- At the start of project work, read the main vault `Obsidian Vault\` (Dashboard + relevant `Projects/` note + `Skills/Claude Skills Index.md`) AND the project's dedicated vault if one exists (e.g. `FamilyLink Vault\`). See [[reference_obsidian_vault]].
- Surface what's relevant: name the project note, the applicable skills, and any gotchas (deploy/account/credentials) BEFORE acting — especially before browser/credential/deploy steps.
- This complements auto-memory; project-specific knowledge lives in the vault (`Knowledge/`) per the vault's own CLAUDE.md, while user/feedback/cross-project facts live in auto-memory.
- Distinction Eduard drew: Obsidian = the archive I must open; auto-memory = what I wake up already knowing. Don't conflate them.
