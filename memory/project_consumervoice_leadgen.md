---
name: consumer-voice-lead-gen-tns-lead-gen-system
description: "React per-company cold-outreach landing pages selling the Consumer Voice platform, live with Supabase backend + Google Calendar booking"
metadata: 
  node_type: memory
  type: project
  originSessionId: db04434f-7f88-481b-8380-fb0448c0e86c
---

# Consumer Voice Lead-Gen — "TNS Lead Gen" system

Per-company **dynamic landing pages** (React) that sell the **Consumer Voice Consumer Community Platform** to FMCG brands, with tracking + secret admin + booking synced to Google Calendar. The user's reusable cold-outreach system, named **"TNS Lead Gen"** (phase 2 = generalize as a template for any business/offer).

**Who:** Engine/product = **Consumer Voice** (consumervoice.eu, reviews/reputation SaaS). Builders = **Wave** (Al Kamara, waveadvertising.ro — commercial face/invoicer) × **The Niche Society** (Eduard, build engine). Landing pages lead with **Consumer Voice branding** (real logo, blue #2f7fe4 + gold #f5a623 + white, heavy Archivo display font); small **Wave×TNS logo-only co-brand lockup** (no text).

**The offer (what the pages sell):** Consumer Community Platform = 4 tools (Verified Reviews & Ratings · Recipe & Inspiration Hub · Purchase-Based Promotions & Contests · Always-On Feedback), each delivering for Marketing/Sales/Trade Marketing. Pitch = brand builds its own proprietary first-party consumer asset. Dev waived for 12-mo deal, €300/brand/mo (volume discounts). PRICES KEPT OFF the landing page (Eduard's call). Source: `Downloads/Consumervoice consumer platform proposal.docx`.

**NDA — CRITICAL:** client platforms (Macromex/Kandia/VICI/Albalact/Caroli/Cris-Tim/etc.) may NOT be shown or named on the page. Only **FrostBite** (own invented demo brand, frostbite-showcase.netlify.app) is shown — as a live iframe preview. Proof = aggregate stats + "built confidentially under NDA" line.

**Code:** `Desktop/claude/tns-leadgen/` (Vite + React + TS + Tailwind v4 + framer-motion, React Bits-style anims). Preview config `tns-leadgen` port 8091. Per-slug route `/:slug` reads `src/data/leads.ts` (mock: /heidi RO, /demo EN). Tailored section auto-cuts if lead has no brands. Real logos in `/public/logos`.

**LIVE:** https://consumervoice-leadgen.netlify.app (Netlify site `consumervoice-leadgen`). Secret admin: `/__cv-control-x7k2`, passphrase `cv-eduard-2026` (CHANGE before go-live; stored in admin_config + Admin.tsx GATE).

**Backend — Supabase `tns-leadgen` ref `brlztibzaeflufqepsrn`** (eu-central-1, $10/mo, org "Eduard Dumitriu Org"). Tables: leadgen_visits, leadgen_bookings, admin_config, google_calendar_creds. RLS = anon INSERT only (PII-safe); reads via edge fns (service-role). Edge fns: `admin-stats` (passphrase-gated analytics), `book-calendar` (creates Google Calendar event + invite). All verified working live 2026-06-18.

**Google Calendar booking = DIRECT API, NOT Zapier.** GCP project `my-project-60463tns-leadpages` (Gmail+Calendar APIs enabled, office@thenichesociety.ro, Internal OAuth, Desktop client 988033177152-...). Refresh token (calendar.events scope) minted via dependency-free `niche-leadpages/scripts/catch-oauth.mjs` (localhost:5599 catcher — works around OneDrive-broken node_modules). Token stored in google_calendar_creds. **Cancel the Zapier paid upgrade** — Zapier was abandoned (canvas editor not automatable + cost).

**Monthly cost:** ~$10/mo core (Supabase; Netlify+Calendar+Gmail free) → ~$30/mo with 3-domain Resend Pro email sending.

**LOGIN DASHBOARD (built + DEPLOYED LIVE 2026-06-18, verified end-to-end on production — login + 5 tabs + 18 contacts load).** Secret route `/__cv-control-x7k2` now = real login (replaces passphrase). Custom auth: `dashboard_users` table (sha256+salt via pgcrypto), `dashboard-auth` edge fn issues an HMAC token signed with SERVICE_ROLE_KEY (7-day exp); `dashboard-api` edge fn (token-gated, one endpoint, actions: list/import/send/status/template_get/template_save). 3 users, password **LG2026!**: al_kamara@consumervoice.eu (role consumervoice), al_kamara@waveadvertising.ro (role wave), office@thenichesociety.ro (role tns / Eduard). Tabs: **Leads** (status table new→queued→sent→opened→booked→bounced→skip + engagement), **Import** (drag CSV/XLSX → SheetJS parse client-side → upsert leads+outreach_contacts), **Mesaj** (editable email template RO+EN + variable guide {{company}}/{{hi}}/{{first_name}}/{{contact_name}}/{{sender_name}}/{{landing_url}}/{{slug}} + live preview; stored in `email_template`, falls back to in-code DEFAULT_TEMPLATE), **Trimite** (multi-select + dryRun preview + 2-step-confirm Send), **Analytics**. New private tables: dashboard_users, outreach_contacts (PII: 18 contacts/17 cos seeded from leads-master.json), gmail_creds (the office@ gmail.send refresh token, migrated from niche-leadpages/.secrets/token.json), email_template, resend_config. Admin chunk lazy-loaded (xlsx code-split out of public bundle). Frontend: src/lib/dashboardApi.ts + src/pages/Admin.tsx + src/pages/dashboard/{Leads,Import,Template,Send,Analytics}Tab.tsx. **Clean URLs: /login + /dashboard** (alias /__cv-control-x7k2; the cryptic one was failing when copied). Import tab also has a **manual single-contact add** form (reuses import action). Mesaj tab has **"Trimite test"** → `send_test` edge action sends ONE real email to any typed address (default = your from-email) to preview/test. **First REAL Gmail send verified live 2026-06-18** (send_test to office@ → msgId 19edaf43896125e0) — full send pipeline works. Sender routing refactored into resolveSender()+sendOne() shared by send + send_test.

**Per-user sending (built):** `dashboard-api` routes by logged-in user's from_email — if == gmail_creds.from_email (office@tns) → Gmail truly from that address (works TODAY); else if resend_config.api_key set → Resend from the user's own address; else → Gmail (office@) with Reply-To = user's address (fallback). Send supports dryRun (returns from/via + preview). Warm-up cap ~20/day (UI) / 60/call (server).

**Email sending = Resend (2026-06-18): account office.the.niche.society@gmail.com, plan PRO ($20/mo paid). Domains VERIFIED 2026-06-19 (both), Resend key STORED in resend_config, Al logs in → sends from his OWN address.** Landing now on **go.consumervoice.eu** (Netlify custom_domain on site consumervoice-leadgen; CNAME from consumervoice.eu; SSL live) → BASE_URL switched to https://go.consumervoice.eu in edge fn + LeadsTab/TemplateTab (no more *.netlify.app in emails). From-name MIME-encoded via encodeAddr() (fixed "Eduard · The Niche Society" mojibake); subject = "propunere pentru {{company}}". **thenichesociety.ro DKIM (google._domainkey) STILL pending the value** (Google masks it — user copies from Admin, I add to DNS); thenichesociety.ro DNS is Netlify-managed (NS1 nsone.net, zone 69a18596e3373d00b7a7a61d → addable via Netlify API, token in %APPDATA%\netlify\Config\config.json). Domain ids: consumervoice.eu (id f3f1226f-10e5-4ba8-b759-66db38e2c592) + waveadvertising.ro (id 0307aa7f-f13d-43b2-a7b4-d8ff0041583d). DNS to add per domain (see `tns-leadgen/RESEND_SETUP.md`): MX `send`→feedback-smtp.eu-west-1.amazonses.com(10), TXT SPF `send`→`v=spf1 include:amazonses.com ~all`, TXT DKIM `resend._domainkey`→unique p= (copy from Resend page; Chrome MCP redacts it), TXT DMARC `_dmarc`→`v=DMARC1; p=none;`. After verify → create Resend API key (Sending) → store in resend_config → Al sends from his own address (routing already built). thenichesociety.ro stays on Gmail. Records on `send` subdomain → existing Outlook mail unaffected.

**Leads = DYNAMIC from Supabase (zero-redeploy).** Public `leads` table (safe fields only: slug/company/industry/country/language/brands — NO contact info). Frontend `src/lib/leadApi.ts` `fetchLead(slug)` reads it at runtime (Landing shows a spinner then renders). Adding/editing leads needs NO rebuild/deploy: edit `Downloads/LEADS WAVE&WEB SEM_18.06.xlsx` → run `python tns-leadgen/scripts/import_leads.py` → it classifies FMCG, enriches brands, and POSTs to the `import-leads` edge fn (passphrase-gated, replace=true) → live instantly. Contact emails/phones stay private in `tns-leadgen/data/leads-master.json` (for the future send script) + the cleaned `Downloads/Consumer Voice - FMCG Leads (cleaned).xlsx`.

**Leads imported (2026-06-18):** 19 FMCG companies live (Nestlé, Mars, Purina, Heineken, Pernod Ricard, Jidvei, Fornetti, Lidas, Fuchs, Life Care, Maresi, Riso Scotti, Paste Băneasa, SC Johnson, Philip Morris, Orkla, Admira, Alka, Lactalis), 13 fresh/untouched. 51 non-FMCG → `niche-leadpages/data/tns-other-leads.json` for the TNS web/SEM page later. Data fixes: "LVMH" contact was actually @jidvei.ro → set to Jidvei; SC Johnson's two entries merged. Lactalis already contacted (flagged). Nav now i18n (EN page = "Book a call"); 4-tools top-accent-line removed.

**Still to do:** change admin passphrase+route; delete test calendar event (23 Jun 14:00 "Test Co"); Resend Pro + 3-domain DNS (CV/TNS/Wave) for email sending; cold-outreach send script (reuse niche-leadpages send.mjs, read leads-master.json); build the separate TNS web/SEM landing page for the 51 non-FMCG leads; Higgsfield Wave×TNS collab logo (optional).

**MULTI-OFFER UPGRADE (2026-06-23):** Dashboard now runs multiple offers from ONE login. (1) **Saved email templates** — new `email_templates` table (id/name/RO+EN subj+body/is_active) REPLACES the singleton `email_template`; dashboard-api v6 actions `templates_list`/`template_save`/`template_activate`/`template_delete`; `getTemplate()` now reads the `is_active=true` row. Mesaj tab = template manager (chips to switch, name field, Salvează / Salvează nou / Folosește la trimitere ★ / Șterge). 2 templates live: **'Consumer Voice (FMCG)' [active]** + **'Meniuri 3D — Restaurante'** (draft). (2) **Lead bundles/groups** — new `bundle` text column on `outreach_contacts` (existing 22 = 'FMCG — Consumer Voice'); `import` action takes a `bundle` param; Leads + Trimite tabs render **collapsible group boxes** (hide/show a group, per-group select-all in Trimite); Import tab has a 'Grup de leads' field. (3) **NEW OFFER — 3D smart menus for lux restaurants** (Eduard's idea): background research agent compiled **50 top Bucharest fine-dining venues, 45 with REAL verified emails** (Casa di David, Kaiamo, Bosco, NOR, Osho, Relais&Châteaux L'Atelier/Le Bistrot, 5★ hotel restaurants Roberto's/JW/Corinthia/Radisson, Kané/éto/NOUA new-Romanian FD, etc.) → `tns-leadgen/data/restaurants-bucuresti.json` + `.csv` + `scripts/import_restaurants.mjs` + `scripts/seed_3d_template.mjs`. 45 imported as bundle **'Restaurante Lux Bucuresti'** (5 no-email: Benihana/Soro Lume/Tuya/Madame Pogany/Aqua = CSV-only). dashboard-api v6 deployed, frontend redeployed to go.consumervoice.eu, verified live via Playwright (both group boxes render). **Step 2 OPEN (before sending restaurant outreach):** the 3D template has NO landing link — the `/slug` pages still serve the Consumer Voice FMCG pitch (wrong for restaurants). Need a **3D-menu demo page + likely its own brand/domain** (consumervoice.eu is FMCG-branded). Current 3D msg drives to a free 10-min demo via reply, so it works link-less, but a demo page converts better. User said "vorbim dupa" = Step 2 to be scoped.

**VIDEO-IN-EMAIL (2026-06-23):** Instead of a per-restaurant landing page, emails carry a clickable **video thumbnail** (deliverability-safe: a poster image linking to the hosted clip; NOT a heavy attachment). Per-template cols `email_templates.video_url`/`video_poster`/`video_label` (migration). `buildEmail()` now returns `{subject,text,html}`; when `video_url` set → **multipart/alternative** (Gmail) / `html` (Resend) with the thumbnail; else pure text/plain (FMCG unchanged). **dashboard-api v7**. Clip hosted on Netlify `public/`: `https://go.consumervoice.eu/demo-3d-menu.mp4` (2.2 MB, 576×1024 vertical) + poster `/demo-3d-poster.jpg` (ffmpeg frame). Mesaj tab gained a **Video section** (url/poster/label + live thumb preview). The **'Meniuri 3D — Restaurante'** template: new copy ("v-am lăsat un demo… stabilim o întâlnire online sau fizică", goal=book) + video wired + **now ACTIVE ★**. Verified live: send_test → edumitriu04@gmail.com (msgId 19ef4a25573db0fd) + Mesaj tab renders the video. ⚠ active-template is GLOBAL (one ★): flip ★ between 'Consumer Voice' (FMCG) and 'Meniuri 3D' (restaurante) before each campaign — **per-send template picker is a TODO** to kill this footgun. Edge-fn deploy note: source now >30 KB → ConvertTo-Json dump exceeds inline limit; split into 2 `\n`-boundary chunks or shrink. Scripts: `update_3d_template.mjs`, `seed_3d_template.mjs`, `import_restaurants.mjs`.

**DELIVERABILITY DIAGNOSIS (2026-06-23):** thenichesociety.ro email AUTH is CORRECT — SPF (`v=spf1 include:_spf.google.com ~all`), DKIM (published at `google._domainkey`), DMARC (`p=none` rua=office@), MX=smtp.google.com; mail-tester 10/10. Found + removed a STRAY duplicate DKIM TXT wrongly on the APEX (Netlify rec id `69dfa5b2fc23b806d14225a1`; kept the correct `google._domainkey` one). **Spam risk is NOT the DNS** — it's **Gmail/Workspace sending + new low-volume domain + cold-outreach pattern**. Contrast comfortmap.ro (which inboxes fine): it sends via **Amazon SES** (`include:amazonses.com`, warmed IPs, likely transactional/opt-in) — not apples-to-apples with cold. Levers to improve cold inbox placement: (1) **activate custom DKIM in Google Admin** (Apps→Google Workspace→Gmail→Authenticate email→Start authentication — long-pending; user was logged into office@ admin 2026-06-23); (2) **Google Postmaster Tools** to monitor domain/IP reputation + spam rate; (3) **slow warm-up** ~10-15/day + drive replies/opens. DON'T move cold outreach to Resend/SES — most ESPs ban cold email (account-suspension risk); for scale use a dedicated cold-email tool (Instantly/Smartlead, rotating warmed inboxes). Netlify DNS zone `69a18596e3373d00b7a7a61d`, token in `%APPDATA%\netlify\Config\config.json`. **First restaurant batch (14) sent via Gmail office@ 2026-06-23.**

**CV LANDING UPDATE (2026-06-24):** Copy reworked (FourTools h2 → "O singură platformă din care brandul tău ascultă consumatorii și crește"; OrgValue eyebrow "Valoare pentru toate departamentele" + h2 "De la marketing la trade, fiecare echipă are de câștigat" + Marketing-column values; hero gained trust line "Deja validat în piață, alături de Intersnack"; CTA subtext). **NDA stance RELAXED — the landing now SHOWS real client logos** via a new **ClientWheel** marquee (`src/sections/ClientWheel.tsx` + CSS `.cv-wheel*` in index.css reusing the `marquee` keyframe), 19 logos sliced from `Downloads/clienti-preze.png` → `public/clients/01-19.png` via `scripts/slice_clients.py` (alpha-projection slicer: MOL/Kandia/Magistra/Intersnack/blue/P&G/Panini/Serpico/Kimberly-Clark/Dr.Reddy's/Cris-Tim/Whirlpool/Sun Plaza/Nordic/Samsung/City Park/Unilever/Schneider/Lidl). Placed before Proof (the demo). Removed the "+14 platforme construite" counter. ⚠ `Proof.tsx` still has the old "names stay under NDA" note — slightly contradictory now that logos show; revisit if user wants. Also: **Tailored section (the lead's-own-brands cards, "Croit pentru {company}") REMOVED** from Landing 2026-06-24 (user wanted the page shorter; `Tailored.tsx` file kept but no longer imported).

**RESTAURANT SENDS + MEDIA (2026-06-24):** 34 restaurant emails sent (14+20) from office@ Gmail. **Bounce audit** via office@ Gmail at `mail.google.com/mail/u/1/` (office@ is authuser **u/1**; readonly works in-browser via Chrome MCP): **7/34 bounced** → marked `status='bounced'`: Heritage (fine.dining@heritage-restaurant.eu), Joseph (info@josephrestaurant.ro), Kaiamo (contact@kaiamo.ro), Le Vivier (bonjour@levivier.ro), Pescarus (events@restaurantpescarus.ro), La Cantine de Nicolai (contact@lacantinedenicolai.ro), Mozzafiato (mozzafiato@mozzafiato.ro). (Other inbox bounces = old May fintech campaign Bunq/Monite/Mondu + tests, ignore.) Hadad corrected → added Iulia Munteanu **pr.jhadad@gmail.com** (status new). **Video UPLOAD-FROM-COMPUTER** built: Supabase Storage **public bucket `outreach`** (anon insert+update+read policies via migration `outreach_media_bucket`); TemplateTab `uploadVideo()` uploads clip → `getPublicUrl` → extracts a poster frame client-side (video→canvas→jpeg) → uploads poster → **auto-saves** into the template (so it sticks). Root-cause the video kept vanishing: a **stale dashboard bundle** save sent no `video_url` → `doTemplateSave` set it to '' → cleared (hard-refresh fixes; re-set via SQL to go.consumervoice.eu/demo-3d-menu.mp4). Subject now **'Propunere Meniu 3D pentru {{company}}'**. Send script: `scripts/send_restaurants_batch.mjs N` (dry-run video+sender safety check, then sends next N `new` in bundle). **EMAIL VERIFIER `scripts/verify_emails.mjs`** (outbound port 25 IS open on this machine ✓): per email does MX lookup + SMTP HELO/MAIL FROM/RCPT TO + random-address catch-all probe → classifies valid / invalid / catchall / unknown(greylist/timeout). Run it BEFORE importing/sending any scraped list. Reality check: of 46 scraped restaurant emails, **~30% were bad** (14 invalid/bounced: heritage/levivier/mozzafiato/kane/robertos = no-MX; kaiamo/pescarus/noeme/jwsteakhouse@marriott/harbour/trattoria/zensushi/lacantine = rcpt-550; josephrestaurant timed out). Only import `valid` (+ cautiously `catchall`); skip `invalid`; `unknown` = greylist/slow, retry later. **Batch-2 (2026-06-24):** research agent found 45 more Bucharest restaurants → verified → imported **36** (`data/restaurants-batch2.json`, `scripts/import_batch2.mjs`; skipped 2 invalid + 2 City Grill dups). Bundle now **~82 contacts**: 24 sent, 14 bounced, ~44 new/ready. NOTE re catch-all: Google Workspace/most servers DO reject invalid RCPT (550) so SMTP verify works for them; persistent greylist-451/timeout = anti-harvesting (mailbox likely fine, real send retries through greylisting).

**Docs:** `tns-leadgen/BACKEND.md`, Obsidian `Projects/TNS Lead Gen System.md` + `Projects/Consumer Voice Platform (Wave x TNS).md`. Related: [[project_tns_lead_pages]] (earlier static niche-leadpages instance).
