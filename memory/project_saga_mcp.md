---
name: project_saga_mcp
description: Read-only MCP server that lets Claude see/query a SAGA C accounting database (DBF files) — for Conta4U bookkeeping automation
metadata: 
  node_type: memory
  type: project
  originSessionId: 7b813f9a-b9e1-4db6-8373-607368bc524d
  modified: 2026-07-28T17:05:21.274Z
---

Eduard wants to move Conta4U SRL's accounting (currently on another laptop, in **SAGA C** desktop) onto the main workspace laptop and build an automated workflow. Plan: ZIP the SAGA data (taken with SAGA CLOSED), unzip on this laptop as a **read-only snapshot sandbox** — the other laptop stays the **source of truth**; corrections flow back as import files, never bidirectional live sync.

**Built (2026-07-22):** a read-only MCP server in `saga-mcp/` (working dir `C:\Users\adumi\OneDrive\Desktop\claude`):
- `saga_dbf.py` — core DBF reader (dbfread, cp852 default, tolerant of missing .fpt/bad bytes). No MCP dep, unit-testable.
- `server.py` — FastMCP server `saga_mcp`, 6 read-only tools: `saga_list_companies`, `saga_list_tables`, `saga_describe_table`, `saga_read_table` (pagination+filters), `saga_search`, `saga_overview` (one-shot DB map — run FIRST on a new snapshot to map cryptic SAGA table names → accounting concepts).
- `inspect_cli.py` — standalone CLI to test reading without wiring MCP into Claude.
- `.venv/` (Python 3.14), `requirements.txt` (mcp[cli], dbfread), `.gitignore` blocks *.dbf/*.zip/data.
- Smoke-tested end-to-end on a synthetic dBase III DBF: reads, paginates, filters, searches; **cp852 diacritics confirmed correct** (â roundtrips; console showing � is display-only).

**Key facts learned:** SAGA C stores each firm as numbered folder `0001/` etc. under `C:\SAGA C.3.0\`; data = DBF (VFP/xBase) + .fpt memo + .cdx index. NEVER write DBF directly (corrupts .cdx). Write path = official file import (`Diverse → Import date`, XML `F_<cif>_<nr>_<data>.xml` / DBF `DF_<start>_<end>.dbf`) or computer-use on the UI. SAGA has NO REST API — integration is the documented file import/export contract (what SmartBill/Oblio use).

**Config:** env `SAGA_ROOT` (point at unzipped snapshot), `SAGA_ENCODING` (default cp852 — VERIFY vs cp1250 against real data's header codepage byte when first real ZIP arrives).

**MAJOR UPDATE (2026-07-22):** SAGA C is ALREADY installed on this laptop at `C:\Users\adumi\OneDrive\Desktop\Conta4u\SAGA C.3.0\` (synced via OneDrive, dated 2023 — Eduard forgot it was here). Contains `FIRME.DBF` + 2 company folders, each 182 DBF tables:
- `0001` = **Advanced Thinking Suport SRL** (CUI 39451359) — mostly empty
- `0002` = **GFM VENTURE SRL** (CUI 38681651) — 53 non-empty tables, real data (1568 journal notes)
These are OLD CLIENT firms (not touched in a while), NOT Conta4U's own books (still on the other laptop). Real client accounting data → keep strictly local (.gitignore blocks all *.dbf).

**Real SAGA C schema mapped** (from live data): `conturi`=chart of accounts+opening balances (CONT,DENUMIRE,TIP,DEB_INIT,CRED_INIT); `registru`=general journal (ID_NOTA,CONT_D,CONT_C,SUMA,TVA,DATA,EXPLICATIE); `intrari`/`iesiri`=purchase/sales invoices (+`intr_det`/`ies_det`); `furnizori`/`clienti`=partners; `note_facturi`=note↔invoice link; `d300`/`d394`/`d394_vanzari`/`d394_cumparari`=declaration tables SAGA already computes (read these, don't recompute).

**Semantic layer DONE & tested on real data (11 tools total):** added `saga_balanta`, `saga_fisa_cont`, `saga_jurnal`, `saga_facturi` (unpaid filter), `saga_parteneri` to the 6 base tools. Verified internally consistent (fisa_cont 5121 sold = balanta 5121 sold = 793,463.90). **Wired into Claude Code at USER scope** (`claude mcp add saga -s user`, in `~/.claude.json`, SAGA_ROOT→the OneDrive path); status Connected; live after a session restart.

**HARD SAFETY RULE (Eduard's explicit instruction):** NEVER validate/submit anything — SAGA auto-uploads declarations to SPV/ANAF. MCP is read-only by design (dbfread, no write path). For declarations: prepare/compute/draft only, the validate+submit-to-SPV click stays a MANUAL human action. Do NOT open SAGA UI or run DUKintegrator on these client firms. (Nothing was ever written to the SAGA folder; SAGA never opened.)

**Declarations layer DONE (2026-07-22, now 13 tools):** `saga_declaratie` (reads d300 form rows + summary: taxa_colectata rand19 / deductibila rand31 / tva_de_plata rand38 / sold_negativ rand46; and d394 vanzari/cumparari totals) + `saga_verificare_tva` (3-way cross-check D300 vs D394 vs journal 4427/4426, tol 1 leu, flags mismatch BEFORE filing). Tested on GFM Venture 2023/08: all_ok True (diffs <0.73 lei). **Server file already has these — restart Claude to get 13 tools live.** SAGA d300 stores VAT return per form ROW (LUNA,AN,RAND,DENUMIRE,VALOARE=baza,TVA); periods there: 2023/07, 2023/08.

**On "upload to SPV" (Eduard asked for the general capability):** SETTLED — automate the whole pipeline up to submission (prepare→validate→generate XML→DUKintegrator local validation+PDF), but the actual depune-to-SPV stays a deliberate per-declaration human action (needs his SafeNet signature token anyway). NO autonomous/fire-and-forget submission, ever. MCP stays read-only.

**Bank layer DONE (2026-07-22, `saga_bank.py`, now 16 tools):** Eduard uses **ING** (exports MT940/MT942/CAMT.052/CSV; SAGA imports MT940 natively). Built MT940 parser from SWIFT spec (no sample needed) + `saga_extras_mt940` (parse → signed transactions, opening/closing balance) + `saga_reconciliere` (match statement vs SAGA 5121 ledger by amount+date±tol, surfaces `unbooked` = in statement not yet in SAGA). Self-tested end-to-end by synthesizing an MT940 from real 5121 lines: matched=4/unbooked=1 correct. ING CSV would need a real sample; MT940 is the clean route (spec-driven).

**SAF-T readiness DONE:** `saga_saft_readiness` — data-quality check (orphan accounts, partners w/o CUI, section counts, ready flag). Does NOT generate D406 XML (SAGA does) or submit. GFM Venture: ready=True, 1 warning (natural-person client, legit).

**FULL TOOL SET (16), live after Claude restart:** base read (6) + accounting (balanta/fisa_cont/jurnal/facturi/parteneri) + declarations (declaratie/verificare_tva) + saft_readiness + bank (extras_mt940/reconciliere). MCP wired user-scope, ✓ Connected.

**CECCAR "brain" DONE (2026-07-22):** Created skill **`ceccar-expert`** at `~/.claude/skills/ceccar-expert/SKILL.md` — MUST be consulted for ANY Conta4U/client accounting or tax work (registrations, D300/D394/D406, bank reconciliation, SAGA queries). It points to the knowledge base in the TNS Obsidian vault (`OneDrive/Documents/Obsidian Vault/Knowledge/`): `Contabilitate CECCAR MOC` + notes (Plan de Conturi General, TVA și Decont, Monografie — Achiziții/Vânzări/Bancă și Casă, and the core **Regula — Cercetare Surse Autoritative**). Linked from Knowledge MOC + Claude Skills Index in the vault. Sources: public authoritative (OMFP 1802/2014, Cod Fiscal L227/2015, ANAF) + Eduard's own CECCAR/internal materials (to be imported). **CORE RULE (Eduard's instruction):** when an accounting/tax treatment isn't clear, DON'T guess — research authoritative sources (Monitorul Oficial → Cod Fiscal → OMFP 1802 → ANAF → CECCAR), verify in force at the operation's date, cite the legal basis, then turn the resolved case into a new monografie note. Brain grows from (a) his materials + (b) each real case solved.

**Scope expansion (Eduard wants, boundary SETTLED):** invoice issuing (e-Factura), making registrations, uploading to SPV "on greenlight." My line: prepare/validate everything to one click; registrations → import files he imports (no direct DBF writes); invoice issue + SPV submit = per-item human greenlight, and his SafeNet signature/PIN stays his (I don't operate his digital signature). Read-only MCP unchanged.

**CONTA4U DATA ARRIVED + VERIFIED (2026-07-22):** ZIP `CONTA4U_migrare_2026-07-22.zip` in Downloads, extracted to `C:\Users\adumi\SAGA-conta4u\`. KEY: Conta4U (CUI 47755391, SAGA folder 0011) uses **Firebird 3.0** (`cont_baza.fdb`, 38MB), NOT DBF like the old client firms. Data at-zi incl. July 2026. Verified directly (read-only): July invoices exact — nr4 FOX COM SERV 14.07.2026 9172.09, nr5 AUTOS WORLD 14.07.2026 5242.86; max REGISTRU date 2026-07-14; 26 clienti, 3 furnizori, 544 note, 49 iesiri, 610 conturi.

**Firebird backend BUILT (`saga_fb.py`):** downloaded Firebird 3.0.12 x64 portable to `C:\Users\adumi\SAGA-conta4u\_fb30\` (fbclient embedded, no service install); pip `firebird-driver`. Work on a COPY at `_work\conta4u_work.fdb` (reference .fdb set read-only); the .fdb came shutdown → `gfix -online`. Charset **WIN1250** (SAGA Firebird stores cp1250 Romanian text — set SAGA_FB_CHARSET). saga_dbf.read_table/list_tables/describe_table now route to Firebird when target is an .fdb (or company alias 'conta4u'/'0011' + env SAGA_FDB). Schema mirrors DBF 1:1 (CONTURI/REGISTRU/IESIRI/INTRARI/FURNIZORI/CLIENTI/D300/D394 + SAFT_* tables) so all 16 semantic tools work on Conta4U unchanged — tested: facturi/balanta/fisa_cont/saft_readiness/parteneri all OK. **MCP re-registered user-scope with Firebird env** (SAGA_FDB, SAGA_FBCLIENT, FIREBIRD, SAGA_FB_CHARSET) + kept SAGA_ROOT for DBF firms. Query Conta4U with company="conta4u".

**"Open in SAGA GUI" — BLOCKED on Eduard's license (per migration docs):** SAGA license is machine-bound + tied to his SAGA account/serial; opening Conta4U in the SAGA app needs him to activate the license on this laptop (possibly ask SAGA support to move it from old laptop DESKTOP-GEURHQ4) + Firebird server. I do NOT enter his license credentials. Verified the data DIRECTLY instead (the real goal). Restore method: SAGA Administrare→Restaurare din `01_BACKUP_SAGA_LA_ZI\...CFB3.zip`.

**Bank statements + note prep DONE (2026-07-22, 18 tools):** ING statements came as **PDF** (not MT940) in Downloads `statements.zip` → `C:\Users\adumi\SAGA-conta4u\_statements\` (4 months Mar-Jun 2026, acct RO09 INGB...1371 3057). Built `saga_ing_pdf.py` (pdfplumber; signs from balance-delta; all 4 reconcile) + `saga_notes.py` (`prepare_bank_notes`: reconcile vs 5121, propose notes per monografii) + MCP tools `saga_extras_ing`, `saga_note_banca`. FINDING: all 12 bank movements were UNBOOKED in SAGA (5121 had ~0 in this period) → that's why all invoices showed unpaid. Prepared 12 notes (8 clear: fees→627=5121, client receipts→5121=4111.<analytic> matched by CUI/name incl. REALITATEA 4111.00024/VIA SARTORIAL 4111.00023; 4 review: 3 associate deposits 5121=455 [confirm 455 vs 4551 — both in chart], 1 AFP garnishment -937 446=5121 [needs debt doc]). Deliverable CSV: `C:\Users\adumi\SAGA-conta4u\note-propuse\note_banca_ING_mar-iun2026.csv`. PREPARED not posted — Eduard reviews/confirms/imports. July ING statement still missing (books have July FOX/AUTOS invoices unpaid).

**FIREBIRD DISCOVERY FIXED + 2 NEW TOOLS (2026-07-28, now 20 tools):** the Firebird backend existed but was effectively unreachable — `_looks_like_company_dir()` required at least one `.dbf`, and a Firebird company folder holds ONLY `cont_baza.fdb`, so Conta4U (now folder **0003** on `E:\SAGA C.3.0`, not 0011) was invisible to `list_companies` and to every tool that resolved a company by number. Also `_firebird_target()` didn't resolve bare folder numbers under SAGA_ROOT, and `search`/`overview` still went straight to DBF files. All fixed; `list_companies` now returns a `backend` field (`dbf`/`firebird`). The semantic layer needed no changes — it all goes through `read_table`, which already dispatched. New tools: **`saga_diagnostic`** (why SAGA's menu is locked: reads `FIRME.PRELUAT` from config.fdb + `SOCIET.PRELUAT/PRELUATS/DATA_INCH` + opening balances, returns probable cause + fix — see [[skill_saga_firebird_direct_read]]) and **`saga_impozit_profit`** (D100 from the journal: 7xx income − 6xx expenses excluding 691, ×16%, per quarter + cumulative; compares against what's booked in 691, benchmarked against the **last closed quarter** not year-end, 1-leu tolerance). Added `smoke_test.py` — runs the whole chain against the real install (`SAGA_ROOT="E:\SAGA C.3.0"`, `SAGA_FBCLIENT=...\_fb30\fbclient.dll`, company `0003`). Verified on live Conta4U: Q1 2026 income 0 (so D100's "suma de scăzut = 0" is correct), Q2 cumulative profit 32,497.12 → tax 5,199.54 vs 5,200 booked in 691, saft_readiness `ready=True`. Works with SAGA OPEN. Vault: [[MCP saga — suport Firebird și unelte fiscale]].

**Next steps:** (1) when Conta4U's own books arrive (Eduard making the ZIP, 2026-07-22) → point SAGA_ROOT at them; (2) import Eduard's CECCAR/internal materials to deepen the brain; (3) build salarii/amortizare/închideri monografii (verify rates via research rule); (4) optional bank txn categorization rules (his mapping, human-validated); (5) invoice-prep + declaration-file generation (prep only). SAF-T/D406 obligation active 2026. Legal IDs: [[reference_conta4u_legal]]. Advanced workspace patterns: [[project_cfo_workspace]]. Vault conventions: [[reference_obsidian_vault]].
