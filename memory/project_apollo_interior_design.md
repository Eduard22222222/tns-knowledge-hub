---
name: project_apollo_interior_design
description: TNS website proposal for warm Instagram lead Apollo Interior Design — 3 tiers (3500/7000 RON + 3D phase)
metadata: 
  node_type: memory
  type: project
  originSessionId: ff7214eb-60e1-4e0d-8733-f6a235010b87
---

Warm lead (Instagram) **Apollo Interior Design** — interior design studio. Eduard called them, wants a website. Built a TNS-format proposal (NOT a contract) in Romanian, gold/cream/serif style like [[project_salt_bank_ecosystem]] and LAGO.

Files in `apollo-interior-design/`: `TNS_Proposal_Apollo.html` (source), `TNS_Proposal_Apollo.pdf` (deliverable), `generate-pdf.py` (pyppeteer+Edge, offline file:// render, no server), `tns-logo.png`, `README.md`.

**3 etapized packages** (each includes the previous):
- **Vitrină — 3.500 RON**: multi-page site, on-page SEO 85+/page, Google Business Profile, portfolio, responsive, full handover (code+domain+accounts).
- **Studio — 7.000 RON**: Vitrină + booking system + RO/EN toggle + direct WhatsApp button.
- **Proiecte 3D** (add-on, VARIABLE per-project — Eduard wanted per-project, "more fair"; renamed from "Faza 3 — Imersiv"): dynamic CMS portfolio + 3D walkthrough/360° per project. Tur 360° 700–1.000 RON/proiect; Walkthrough 3D interactiv 1.500–2.500 RON/proiect. Dynamic-portfolio + viewer activation = small one-time cost INCLUDED in first project (no big setup anchor). Each project quoted individually after seeing source file. Rationale: designers already have 3D models (SketchUp/3ds Max/Lumion) → work = web-optimizing them.

**SEO target = ALL OF ROMANIA.** Apollo is in **Iași** but most projects are in **București**. Honest strategy in proposal: GBP wins Iași (local pack = physical HQ); dedicated city pages + organic SEO cover București + rest of country (cities with no physical office). Keyword table includes "design interior București" (high vol) + "design interior Iași" (HQ) + national terms.

Terms baked in: 50% avans / 50% la lansare, 30-day validity, hosting free-tier (Netlify/Vercel), domeniu ~50–70 RON/an, optional maintenance.

Notes: Ahrefs keyword data unavailable (API "Insufficient plan") → SEO table uses labeled market estimates + promise of keyword audit at kickoff. PDF pipeline = same as SaltBank but loads local file directly. At sign-off: provision dedicated Obsidian vault + isolated Supabase if backend added (per [[policy_supabase_isolation]]). Style follows [[feedback_proposal_style]] + [[feedback_quality_standards]].
