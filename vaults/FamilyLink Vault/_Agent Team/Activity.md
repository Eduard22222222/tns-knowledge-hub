---
tags: [agent-team, activity]
project: FamilyLink
created: 2026-05-04
status: active
---

# Activity Log

- 2026-05-04: Agent Team coordination layer spawned for FamilyLink.
- 2026-05-18: Live production audit + 3 fix rounds on comfortmap.ro (Sentry crash, real data wiring, weekly reporting, magic-link login fix + regression caught live, onboarding self-service doc delivered & live-tested, .env rebrand aligned). Full detail → [[ComfortMap Live Audit 2026-05-18]]. New runbook → [[Onboarding Cămin Nou — Ghid Super-Admin]]. Open: handover access (staged for demo date), Calendly account, GitHub PAT rotation.
- 2026-05-19/20: Per-staff resident assignment feature shipped end-to-end in 4 atomic steps (migrations 0008+0009 + 5 commits): DnD board admin, scoped staff dashboard, role-aware UI on 10 pages, RLS closed at DB level (residents/journal/messages/families). 22+17 live verification checks per role, R4 fix preserved. Full detail → [[ComfortMap Live Audit 2026-05-18]] ROUND 5.
- 2026-05-19: Pre-handover presentation prep. Re-verified prod live (deploy 43b083e READY, all 5 demo accounts password-login OK, public pages 200). Delivered → [[Demo Script & Live Presentation Runbook]] (3-act live flow, credentials, technical talking points, what-not-to-touch, Q&A).
- 2026-05-19/20: B.1–B.3 list-first scale UI shipped (commits 07e55ef → f4fa453). View-mode toggle persistă în `users.ui_preferences` JSONB, smart default ≤6 staff = board / >6 = list. Search + 3+N filter chips + multi-select bulk + tap-chip popover reassign + bulk actions (Distribuie egal / Mută toți / Reset zi / Dezalocă toți per staff). Bug "Mama Maria rămâne alocată" raportat de Eduard → root cause `revalidatePath` lipsă pe sub-rută `/caminul/echipa/alocari` + local-state divergence în `assignment-list.tsx` fără sync pe props. Fix în commit 22a093e: revalidate ambele căi în `assignStaffAction` + `unassignStaffAction`, `useEffect` sync prop→state, plus `UnassignAllButton` (cerere Eduard "buton de dealocare pe fiecare asistent"). Verificat live end-to-end: setup via service_role, click în UI, DB query confirmă `removed_at` set la +54s, UI re-render fără F5. Sistem alocări **production-ready pentru handover**. Detail → [[ComfortMap Live Audit 2026-05-18]] R6.1–R6.7.
- 2026-05-20: B.4 — Filtre + sort + N+1 fix pe /caminul/rezidenti (commit 1e7a17e). Pagină rezidenți primește același pattern UX ca alocările: search debounced 250ms, view toggle Listă/Grid, sort dropdown (alfabetic/recent/cameră), filter chips (Toți / Update azi / Fără update săptămâna asta / Cu note speciale) + Per cameră cu dropdown sub-menu, counts contextuale post-search, empty states context-aware. Tot state persistat în URL (shareable + back/forward). În spate: `listLatestJournalByResidentForTenant()` înlocuiește N+1 (un journal-fetch per rezident → un singur query bulk reduce-uit în Map). Verificat live admin: toate filtrele + search debounce + room dropdown funcționează, console clean. Detail → [[ComfortMap Live Audit 2026-05-18]] R6.8. **Next**: voice transcribe cu Whisper API (B.5).
- 2026-05-20: B.5 — Voice transcribe în composer journal cu OpenAI Whisper (commit 2ba0197). Cheie restricționată "FamilyLink Whisper Prod" creată în OpenAI dashboard (Model capabilities=Request, restul=None), push la Vercel env. /api/transcribe primește audio Blob → forward la whisper-1 cu language=ro → returnează text. Voice input client component cu MediaRecorder (state machine idle/permission/recording/uploading/error), pulse animation, mime negotiation cross-browser. Composer refactor: textarea controlată cu body state + buton 🎤 sub textarea + char counter. Auto-punct între dictări consecutive. End-to-end pipeline test live via JS Chrome MCP: 1.1s silence webm (646b) → POST /api/transcribe → round-trip 1165ms → HTTP 503 cu mesaj RO friendly (mapare 429 insufficient_quota). **BLOCKER**: contul OpenAI Personal = $0 balance, user trebuie să adauge credit ($20 starter + auto-recharge $30/lună cap recomandat). Tooltip GDPR mini pe buton, full consent flow deferred la B.6. Detail → [[ComfortMap Live Audit 2026-05-18]] R6.9.
- 2026-05-21: Sprint follow-up Eduard. (1) **B.5 verificare cu credite reale**: $10 adăugat → test TTS→Whisper round-trip cu phrase "Astăzi Mama Maria a mâncat foarte bine la prânz și s-a plimbat în grădină" → 93% word match (Whisper-1 RO excellent, doar diacriticele de final + capitalizare lipsesc). Pipeline 100% verified end-to-end. (2) **B.6 GDPR full voice** (commit 43c5990): consent dialog one-time la prima apăsare pe „Vorbește" cu Shield icon + 3 bullet reasurance + link la privacy 4.A, localStorage flag per browser. Privacy policy extins: section 4 cu bullet OpenAI + sub-section 4.A „Procesare vocală opțională" cu 5 puncte (TLS, no storage, GDPR art. 6, SCC/DPA art. 46, revocare). Anchor #voice pentru deep-link. (3) **C.1 discharge rezident** (commit 7935664): migration 0011 adaugă discharged_at + discharged_by + discharge_reason cu CHECK constraint consistency. Service + Server Action admin-only cu audit GDPR (acțiune nouă 'discharge'). DischargeDialog UI cu 4 motive preset + textarea „Altele" pentru cazul liber. Rezidenții arhivați dispar din lista activă dar URL-ul rămâne accesibil pentru audit; banner amber pe pagină arată motivul + data. (4) **C.2 cancel cron + email cleanup** (commit 5cb8650): decizia Eduard să nu mai trimitem email per-update (email fatigue + cost inutil). Migration 0012 migrează frequency='daily' → 'weekly', schimbă default. postJournalEntryAction șters blocul de email per-update. Preferințe UI redus la 2 opțiuni curate (Săptămânal recomandat / Niciodată). Update-uri zilnice rămân doar în aplicație → engagement. Cost: ~18k emails/lună → ~2.4k/lună (sub free tier Resend). (5) **C.3 pagination 50/page** (commit 7845fac): numeric paginator stil shop la bottom (Afișați 1-50 din 61 · Pagina 1 din 2 · [< 1 2 >]), smart truncare cu ellipsis la >7 pagini, slice server-side după filter+sort, counts globale rămân corecte, filter/sort/search change resetează la page=1, clamp pe interval valid (?page=999 → ultima reală). Verificat live cu 60 dummy rez seed-uite temporar, paginator apare corect la 61 rez, ascuns la 1. (6) **D.1 Sentry audit**: 0 erori în ultimele 24h. 1 issue veche pre-sprint (FAMILYLINK-3 Server Components error pe /login → /caminul/echipa/invita, 2 events 7d, 0 distinct users, Edge browser only, pre-sprint releases 1ffc34861893+43b083e4c02b). Concluzie: toate cele 7 deploy-uri din sprint sunt CURATE — zero erori introduse. (7) **D.2 Fix FAMILYLINK-3** (commit 16336d1): Eduard a vrut totuși să rezolve. Nou helper `requireStaffSession()` în auth.service.ts care în loc de `throw new Error("Sesiune fără tenant")` face redirect curat — family user → /portal, no access → /login?error=no_access, staff cu access → returnează {session, tenantId, access}. Migrate 12 pagini sub (staff)/caminul/* cu același pattern destructure `{ session, tenantId, access: myAccess }`. Pattern uniform, backward-compat pe variabila myAccess. Build verde, TSC clean. Total sprint: 8 deploy-uri (1e7a17e → 16336d1), zero regressions, 1 bug pre-sprint fixed. (8) **E Chat privat familie↔admin** (commit e1de596): canal nou izolat de mesageria zilnică. Decizii Eduard: nominalizat (admin vede identitatea+relația+rezidentul), per familie (un thread global), invizibil staff. Migration 0013 cu tabel family_admin_messages + enum sender_role + SECURITY DEFINER helper is_family_member_of + RLS strictă (zero rows pentru camin_staff). Service+repo cu defense-in-depth ownership checks. UI familie: card CTA Shield pe /portal home + /portal/admin-chat thread DM-style cu bubble blue/white. UI admin: nav item "Conversații private" Lock icon (admin-only via role-nav) + /caminul/conversatii-private cu listă conversații (unread sus) + thread view cu bubble navy/blue + header "Andrei Test Family · fiu · familia rezidentului Mama Maria (cam. Test-101)". Verificat live E2E cu 2 mesaje seed family → admin opens thread (read_by_admin_at auto set) → reply admin → DB confirmă 3 rows cu read flags corecte → cleanup. Sprint total: 9 deploy-uri în 2 zile, 3 migration-uri DB, zero erori introduse, 1 bug rezolvat, 30 tasks completed.
- 2026-05-22: Planning Faza 2 (Eduard prep pentru discuție Cerasela vineri). Două documente noi: [[Faza 2 — Planning 2026-05-22]] (re-scope split 2A revenue / 2B clinical, pricing pe split €7k+€7k, cadru decizie cash-vs-equity, prep complet discuție Cerasela cu întrebări) + [[ComfortMap Architecture]] (deep-dive tehnic modulul A: rute ISR /camine, tabel tenant_public_profiles, Mapbox, SEO, plan build 2.5 săpt). Insight cheie: sprintul de polish a de-riscat părți din Faza 2 (voice→M1, discharge→M2-LITE). Recomandare: split Faza 2 în 2A (ComfortMap+Stripe+Profile, low-risc, începe imediat) și 2B (jurnal+dosar+constante, high-risc, după DPO+avocat MedTech). TASK-0005 caregiver assignment marcat **done** — livrat de fapt în sprintul de polish (resident_assignments + DnD board).
- 2026-05-22: Incident email la onboarding căminul "Acad. Nicole Cajal" — 3 probleme diagnosticate + reparate. (1) **Buton magic link invizibil în Yahoo** (commit 6122cb4): `background:linear-gradient` shorthand → Yahoo ignoră toată proprietatea → text alb pe alb. Fix bulletproof button cu `bgcolor` attr + `background-color` solid. (2) **`@realitateasociala.ro` = domeniu inexistent** (NXDOMAIN) — emailurile către marylou@/cerasela@realitateasociala.ro blocate la `sent`; adrese fictive din mock data. Cerasela primește pe Yahoo-ul ei real. TODO: corectează emailurile seed. (3) **Link invitație "invalid sau expirat"** (commit c377afa): scanerele anti-phishing (Microsoft Safe Links) urmăreau link-ul GET din `/auth/confirm` → consumau token-ul one-time. Fix: butonul devine `<form method="POST">`, handler POST nou în `/auth/callback` = singura cale care consumă token; GET cu token_hash redirectează la confirm. Verificat live end-to-end: token supraviețuiește la 2 GET-uri (scaner), consumat doar la POST (uman). Detalii → [[ComfortMap Live Audit 2026-05-18]] R7.
- 2026-05-22: Root-cause complet FAMILYLINK-3 (commit d311423, cerere Eduard „fully resolved"). Cauza reală mascată de Next.js: `createTenantAction` făcea `throw new Error` la orice eroare de validare formular → crash page + event Sentry. „Intermitent" = doar la input invalid (creare Cajal de pe mobil cu telefon în format greșit). Fix: action returnează `{ok,error}` structurat (nu throw), formular nou client component cu `useActionState` + banner roșu inline, createTenant DB failure prins, getTenantDetail wrapped try/catch. Verificat live: telefon `0712...` greșit → banner roșu „format E.164", zero crash, zero event Sentry, niciun tenant junk. FAMILYLINK-3 marcat Resolved cu cauză identificată + fix dovedit. Detalii → R7.4.
- 2026-05-22: Magic link — invitațiile către conturi NOI eșuau cu „link invalid sau expirat", reparat (commit `dc27f5f`, F.3). Root cause reprodus determinist live: `admin.generateLink({type:'magiclink'})` întoarce `verification_type:"signup"` la PRIMA invitație (email inexistent → Supabase creează userul tăcut, token în coloana `confirmation_token`) dar `"magiclink"` la a 2-a (user deja existent). `magic-link.service.ts` hardcoda `type=magiclink` în URL → la prima invitație `verifyOtp` căuta în coloana greșită → eșec; re-invitația se potrivea → mergea. Asta explică „la unii merge, la alții nu" raportat de Eduard la onboarding-ul căminului Cajal (Bogdan/ireneai/eduard-stud picau, Cerasela/Irina/dr.Iulia mergeau) — depindea exclusiv de reinvitare, NU de Yahoo-vs-Gmail sau scanere anti-phishing (coincidență că reinvitații erau toți pe Yahoo; teoria „conturi noi" era incompletă — corect e „prima invitație vs reinvitație"). Fix: citește `properties.verification_type` real pe ambele ramuri (magiclink+invite). 4 teste determinist pe comfortmap.ro: progresie token signup→magiclink→magiclink; 1ª-invitație+cod-vechi eșuează; re-invitație+cod-vechi merge; 1ª-invitație+fix → 303+cookie /portal. Toate cele 6 căi de invitație (familie staff→owner, familie owner→membru, staff, 2× admin super-admin, login public) trec prin `sendAdminMagicLink` reparat — niciuna nu pre-creează userul. **Gotcha de deploy**: repo are 2 remote-uri — `origin` (Eduard22222222, rulează CI) și `niche` (thenichesociety, de unde deployează Vercel). Push doar pe origin → CI verde, ZERO deploy. Fix-ul a ajuns live abia după `git push niche main` (dc27f5f READY confirmat via Vercel API, team family-link). Conturi blocate de reinvitat: bogdan@buftealakeresort.ro, dumitriueduard22@stud.ase.ro, ireneai020504@gmail.com. Separat de bug: `cerasela@realitateasociala.ro` = NXDOMAIN (adresă fictivă seed) + mail în spam = reputație domeniu comfortmap.ro. Detalii → [[ComfortMap Live Audit 2026-05-18]] R7.5.
- 2026-05-24: **Sprint H (6+1 puncte vineri Cerasela) + Sentry FAMILYLINK-3 închis definitiv.** Un singur deploy (`niche main` → SHA `3167ee0` READY), 5 commits, 2 migrations DB aplicate live pe prod via pg direct. (1) **H.2 + H.4** (`f20b919`) — rename labels „Note interne" → „Particularități și preferințe" + `camin_staff` UI label → „Personal responsabil" în 8 locuri (rol DB neschimbat). (2) **H.3** (`c159e1b`) — composer journal cu „Tip update" vizibil + tags ca „Despre ce e mesajul? max 5 categorii" + helper text explicit. (3) **H.5** (`a577db2` + migr `0014_admin_superadmin_chat.sql`) — canal privat admin cămin ↔ super-admin: tabel `admin_superadmin_messages` cu 6 RLS policies, UI `/caminul/suport` (single-thread admin) + `/platforma/conversatii` (listă cross-tenant super-admin cu unread sus) + `/platforma/conversatii/[tenantId]` (thread per tenant). Pattern mirror al E (familie↔admin) cu un thread per TENANT în loc de per family. (4) **H.6** (`3654073` + migr `0015_family_hub.sql`) — Hub Familial Faza 1 (medicația rămâne Faza 2 / Art. 9 GDPR): două tabele `family_hub_messages` + `family_visits` cu helper SECURITY DEFINER `is_family_of_resident(uuid)` care acoperă owner + members via `families` table. UI `/portal/hub` cu chips membri + visit log („Am vizitat" expand → datetime + nota max 500) + chat composer. Nav item „Familia mea" pe toate cele 6 pagini family. Service layer cu `assertFamilyOfResident()` defense-in-depth pe fiecare apel. (5) **H.7 Sentry FAMILYLINK-3** (`3167ee0`) — alerta nouă mid-sprint pe release `5acebea` (banner stale-build): event Chrome Mobile iOS 148, transaction `/platforma/camine/nou`, „Regressed" (D.2 a wrappat doar `(staff)/caminul/*`, nu și `(super-admin)/platforma/*`). Nou helper `requireSuperAdminSession()` identic cu `requireStaffSession()` — redirect curat: session expirat → `/login?error=session_expired`, user logat fără super_admin → portal natural (staff `/caminul`, family `/portal`), nu expune existența `/platforma`. Migrat 12 pagini super-admin. **Bug script refactor batch:** regex import nu a matchat → 12× `error TS2304: Cannot find name`. Fix manual via Edit. Lecție: după batch refactor, `tsc --noEmit` ÎNTOTDEAUNA înainte de commit. Verificat: `tsc` clean, `next build` exit 0 (41 rute), migrations confirmate aplicate pe prod (pg direct query — 3 tabele + RLS + helper), 5 rute critice live testate (toate 307, zero 5xx), live click cu user real (Andrei family → `/platforma/camine/nou` → redirect curat la `/portal`; `/portal/hub` render perfect cu visit button + composer, 0 error banners). Sentry FAMILYLINK-3 marcat „Resolved in version 3167ee027435" (auto-reopen ONLY pe versiuni ulterioare). Feed Sentry `is:unresolved` 24h → „No issues match your search". **Stare:** 0 issues active, 7 puncte rezolvate într-un deploy, 0 regresii noi. Detalii → [[ComfortMap Live Audit 2026-05-18]] R7.6.
- 2026-05-24: **Verificat weekly report email pipeline live + TASK-0004 deferred la Faza 2.** Întrebare Eduard: „știu că merge in-app, dar pe mail nu știu dacă se trimite". Auditat end-to-end: (a) cron `vercel.json` configurat LUNI 05:00 UTC, (b) cod `api/cron/weekly-reports/route.ts` cheamă `sendEmail({category:'weekly_report'})` în loop pe familii reale + skip `frequency='never'`, (c) DB confirmă cron a rulat real pe 2026-05-18 05:00 UTC (7 rândurile generate exact la oră), (d) 15/25 rapoarte cu `sent_to_family_at` populat (cele 10 fără sent = rapoarte cerute on-demand via buton, NU se trimit pe email — corect, familia e deja în app), (e) **Resend dashboard confirmă 8 livrări REALE „Raport săptămânal — ..." cu status `Delivered`** la familii Cajal pe 2026-05-22+23 (liliana.folea, irina.vasilescu, ireneai020504, dumitriueduard22, marylou@europe, mariacerasela_maciuca, bogdan@buftealakeresort). Sistemul funcționează 100%. Singurul gap: template-ul HTML e basic (doar headline + count + CTA), TASK-0004 cere upgrade la „profi" (hero photo, AI summary, highlights cards, foto strip). **Decizie Eduard:** „ține aia pentru Faza 2" — îmbunătățire UX, nu blocker funcțional. TASK-0004 frontmatter updat: `status: deferred`, `phase: 2`, `deferred_at: 2026-05-24`, `deferred_until: "Faza 2"`. Clarificare colaterală: emailurile „Update-uri despre X de la Y" văzute în Resend NU sunt notificări per-update (acelea sunt killed în C.2) — sunt subject-ul invitațiilor magic link la familie nouă (`inviteFamilyAction`, category=`family_invite`).
- 2026-05-24: **Sprint I: revocă acces angajat — admin poate scoate staff din echipă** (commit `921c719`, migration `0016`). Întrebare Eduard: „ca admin îmi trebuie buton să șterg angajați și pacienți (demisie, deces, greșeală)". Audit: rezidentul are deja discharge din C.1 (4 motive preset, audit GDPR), dar staff-ul nu avea cale de revocare din UI — `revoked_at` doar coloana filtrată în queries, fără buton. Production-blocker pentru cămine reale. (1) **Bug colateral găsit + fixat**: TS type `AuditAction` include `"discharge"` din C.1, dar SQL ENUM `audit_action` din 0001 nu fusese extins → INSERT-urile audit cu action='discharge' eșuau silenţios pe `audit_log` (rămâneau doar în `mockAuditLogs` JS array). Migration `0016_audit_action_extensions.sql` aplicată pe prod (pg direct + IF NOT EXISTS) adaugă 'discharge' + 'revoke'. Gotcha proces: script-ul de migration cu split naiv pe `;` + filtru `!startsWith('--')` a tăiat primul ALTER pentru că era atașat la comment block → rerun direct cu query. (2) **Service `revokeTenantMember()`** în `tenant-members.service.ts`: assertRole admin/super + anti-lockout (caller nu poate revoca propriul acces) + anti-orphan (ultimul `camin_admin` nu poate fi revocat — tenant ar rămâne fără admin) + cleanup `resident_assignments` via `unassignAllFromStaff()` (pacienții rămân fără asistent — admin realocă manual) + setează `user_tenant_access.revoked_at = now()`. NU șterge user-ul nici datele postate — istoricul rămâne pentru audit GDPR (5 ani contract). Idempotent pe deja-revocat. (3) **`revokeStaffAction()`** în `(staff)/_actions.ts`: validare FormData (4 reason codes + textarea opțional pentru „Altul"), defense-in-depth role check, audit log cu metadata complet (reason, assignmentsRemoved, revokedUserName), revalidate pe `/caminul/echipa` + alocari + acasă. Suffix dinamic în mesaj UX: „N pacienți au fost dezalocați — realoacă-i altui asistent". (4) **`RevokeMemberDialog`** client component (~200 linii, pattern mirror al DischargeDialog): buton compact roșu pe fiecare card de membru activ, modal cu warning GDPR + warning pacienți alocați + 4 motive radio (Demisie / Concediere / Greșeală / Altul), submit destructive, auto-close 1.8s după success. Disabled cu tooltip pentru ultimul admin. (5) **Wire în `/caminul/echipa`**: query nou pentru counts alocări per staff (1 query bulk), calculează `isLastAdmin` per membru, badge cu nr alocări pe card-uri, etichetă „(tu)" lângă propriul nume, butonul NU se afișează pentru propriul acces (anti-lockout UI). Verificat: tsc clean, next build exit 0, migration `0016` aplicată pe prod (`enum_range` returnează toate 9 valori). Deploy `921c719` READY pe `niche` + `origin`. 1 commit, 7 fișiere, +526 linii, -1. Detalii → [[ComfortMap Live Audit 2026-05-18]] R7.7.
- 2026-05-24/25 noapte: **Sprint J/K/L/M — 6 deploy-uri rapide pe bug reports live de la Eduard.** (1) **J.1 password wording** (`47891ff`) — clarificat că PasswordSetupCard funcționează pt SET + CHANGE (Eduard credea că familia n-are loc să schimbe parola; era deja, doar wording-ul „Setează" sugera prima dată). Mutat password card pe `/portal/preferinte` mai sus (sub email prefs, înainte de invite member). (2) **J.2 CRITIC magic link → homepage** (`a8507e1`) — Eduard raport „mi a venit si m a trimis pe website ce mama dracu". `/login/actions.ts` trimitea intenționat `next:"/"` pentru role-based routing, dar `/auth/callback` accepta `"/"` ca destinație literală. Fix: condiția cere `next.length > 1`. Verificat live cu 3 conturi: marylou super_admin → /platforma, office@fiblyai admin → /caminul, family → /portal. (3) **K.1 access sort accepted_at DESC** (`95feb01`) — Marylou nu vedea PROBA în /caminul pentru că Postgres returna `camin_admin @ Carmen` (vechi) înainte de `camin_staff @ Cajal` (recent). ORDER BY accepted_at DESC → cel mai recent câștigă. (4) **K.2 regression multi-role same-tenant** (`3d0c29b`) — Cerasela admin Cajal nu putea intra în Echipa după K.1 deploy: avea `family_owner @ Cajal` (acceptat recent) + `camin_admin @ Cajal` (acceptat înainte). K.1 sort a pus family primul → pattern `find(a => a.tenantId === ...)` returna family → role check pică. Fix: pages folosesc `access` direct din `requireStaffSession()` destructure (deja returnează rolul staff corect); actions filtrează explicit `find(role === admin/super)`. Verificat live cu Cerasela: /caminul/echipa + invita → 200 cu buton/formular prezent. (5) **L.1 List-Unsubscribe + List-Id headers** (combined cu 3d0c29b) — adăugat header-ele RFC 8058 pentru categoriile bulk (`weekly_report`, `daily_update_notify`) + endpoint `/api/unsubscribe` (GET/POST one-click). Gmail/Yahoo/mail.com afișează acum buton „Unsubscribe" lângă subject + clasifică sender ca „bulk responsabil". (6) **M.1 password card mobile wrap** (`1042ad8`) — helper text se rupea urât în dreapta butonului pe Cerasela (~360px); fix flex-col→sm:flex-row. (7) **M.2 timezone Europe/Bucharest** (`99e1cfd`) — Eduard observ that toate orele afișate erau UTC (Vercel server), nu RO. Script bulk a adăugat `timeZone: "Europe/Bucharest"` la 35 call-uri `toLocale*String` în 18 fișiere + helpers noi în `dates.ts`. Update postat la 22:30 RO va afișa „22:30" în loc de „19:30" (cum era înainte). (8) **Mail-tester audit** — emailul nostru de invite obține **SCORE 10/10** la mail-tester.com (SpamAssassin standard). DNS + content + auth = perfect tehnic. Spam-ul Marylou e cauzat de reputație domeniu nou (3 săpt vechi) + filtru agresiv mail.com — se rezolvă organic în 2-4 săpt cu volum legitim. Acțiunea cu impact max: cere clienților să marcheze „Not Spam" + reply când primesc primul email. **Stare seara asta:** 0 Sentry issues unresolved 24h, 10 deploy-uri stabile, Faza 1 production-grade pentru demo larg.
- 2026-05-24: **J.1 — wording clar pentru schimbarea parolei pe toate dashboarduri** (commit `47891ff`). Eduard observă „familia parcă nu are loc să-și schimbe parola". Verificat live: `PasswordSetupCard` EXISTĂ pe toate 3 dashboarduri (`/caminul/setari`, `/portal/preferinte`, `/platforma/setari`) ȘI funcționează identic pentru SET + CHANGE (`supabase.auth.updateUser({password})` tratează ambele cazuri). Problema = wording „Setează o parolă" sugera că funcționează doar prima dată. Schimbări: (a) `PasswordSetupCard` defaults — title „Parolă cont" + description „Setează SAU schimbă parola" + helper text „funcționează la prima setare ȘI la schimbare", (b) `set-password-action.ts` success message neutru „Parolă salvată" (nu „setată"), (c) wording uniform peste cele 3 call sites cu „Setează sau schimbă", (d) **mutat password card mai sus** pe `/portal/preferinte` — după email prefs, ÎNAINTE de invite member section (era jos de tot, scroll lung pe mobile). Zero schimbare funcțională. tsc clean, deploy `47891ff` READY. 5 fișiere, +19/-18 linii.
- 2026-05-24: **J.2 CRITIC — magic link de pe /login te trimitea pe homepage** (commit `a8507e1`). Eduard raport live: „login ul prin magic link daca vreau sa ma loghez mi a venit si m a trimis pe website ce mama dracu". Root cause în `/auth/callback/route.ts` `completeLogin()`: filtrul `if (next && next.startsWith("/") && !next.startsWith("/login"))` accepta `next === "/"` ca destinație literală → user ateriza pe homepage public. `/login/actions.ts:185` trimitea intenționat `next: "/"` cu intenția „rezolvă rolul automat" (comentariul din cod chiar așa explică), dar callback-ul nu gestiona special cazul "/". Bug LIVE de când F.3 a mutat login-ul public pe magic link branded (commit `dc27f5f`, 2026-05-22) — **~2 zile întregi cu UX rupt pentru returning users**. Invitațiile inițiale (familie/staff/admin) NU erau afectate — alea trimit `next: "/portal"` sau `"/caminul"` explicit. **Fix:** condiția cere acum `next.length > 1` → `"/"` cade în ramura role-based ladder (super > admin > staff > family). Verificat LIVE cu 3 conturi reale: `office+familytest@` → `/portal` ✅, `office@fiblyai.com` → `/caminul` ✅, `marylou@realitateasociala.ro` → `/platforma` ✅. Cookie sesiune setat în toate cazurile. Singurul `next: "/"` din cod e în `/login/actions.ts` (login public form) — celelalte 5 call sites trimit deja path explicit, deci fix-ul nu schimbă comportamentul lor. 2 fișiere modificate, +16/-1 linii. Deploy `a8507e1` READY pe `niche` + `origin`. Sentry post-deploy: 0 issues. Detalii → [[ComfortMap Live Audit 2026-05-18]] J.2.
